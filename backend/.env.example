# CalAnywhere Backend Configuration
# Copy this file to .env and fill in the values.

# ── Required: Database ───────────────────────────────────────────
# Omit to run in ephemeral in-memory mode (no data survives restart).
# Set to enable persistent storage with PostgreSQL.
# DATABASE_URL=postgresql://calanywhere:changeme@localhost:5432/calanywhere

# ── Required for notification emails: Encryption ─────────────────
# 64-character hex string (32 bytes) for AES-256-GCM encryption of
# notification email addresses stored in the database.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# EMAIL_ENCRYPTION_KEY=

# ── Optional: Email (Mailgun) ──────────────────────────────────
# When set, appointment verification emails are sent via Mailgun.
# When absent, emails are logged to the console (fine for local dev).
# MAILGUN_API_KEY=
# MAILGUN_DOMAIN=
# MAILGUN_FROM_EMAIL=
# MAILGUN_API_BASE=https://api.eu.mailgun.net

# ── Optional: Server ─────────────────────────────────────────────
# PORT=4000
# ALLOWED_ORIGIN=*
# BASE_PUBLIC_URL=http://localhost:5173
